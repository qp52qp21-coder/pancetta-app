# パンチェッタ熟成手帳

自家製パンチェッタの仕込みから完成までを記録する、スマホ向けWebアプリ。
個人利用（開発者本人＋別居の子ども）。ビルド不要の静的サイト。

## 技術構成

- 単一の `index.html` に HTML / CSS / JavaScript をすべて内包（フレームワーク・ビルド不要）
- データ保存は `localStorage`（キー: `pancetta-batches`）。サーバーなし、通信なし
- `manifest.json` により Android Chrome で「ホーム画面に追加」するとアプリ風に起動
- 想定端末は Google Pixel（Android / Chrome）。iPhone でも動作する必要あり

## ファイル構成

```
index.html      アプリ本体（これ1つで全機能）
manifest.json   ホーム画面追加用の設定
icon-192.png    アイコン（192px）
icon-512.png    アイコン（512px）
```

## 公開先

- リポジトリ: https://github.com/qp52qp21-coder/pancetta-app
- 公開URL: https://qp52qp21-coder.github.io/pancetta-app/
- GitHub Pages（main ブランチ / root）で公開。`git push` すると1〜3分で反映される

## データ構造

```js
batch = {
  id,          // 文字列（Date.now().toString(36)）
  name,        // 仕込み名
  startDate,   // "YYYY-MM-DD" 仕込み開始日
  d1,          // 塩漬け日数（既定7）
  d2,          // スパイス熟成日数（既定7）
  saltPct,     // 塩の割合(%)
  sugarPct,    // 砂糖の割合(%)
  meatWeight,  // 仕込み時の肉重量(g)
  spice,       // スパイスメモ（文字列）
  weights,     // [{date:"YYYY-MM-DD", grams:数値}, ...] 重量記録の履歴
  status,      // 'curing' | 'done'
  rating,      // 1〜5（完成時の★評価）
  memo,        // 味のメモ
  doneDate     // 完成日 "YYYY-MM-DD"
}
```

全 batch を配列にして JSON 文字列で localStorage に保存している。

## 主要な機能

1. **配合計算** — 肉の重量から塩・砂糖のグラム数を自動計算。プリセットは「塩4%/砂糖0.5%」と「塩3%/砂糖1%」の2種類、%の手入力も可
2. **熟成カウントダウン** — 円形リングで「N日目 / 全日数」を表示。工程は「塩漬け中 → スパイス熟成中 → 熟成完了」の3状態。完成予定日を表示
3. **重量記録** — 日付ごとの重量を追加し、初回比の減少率(%)を表示
4. **完成アーカイブ** — ★5段階評価と味メモを残し、次回の配合改善に使う

## 設計ルール（変更時も必ず守ること）

- **配色は3色のみ**
  - 生成り `#FAF5EE`（背景）
  - 濃茶 `#3B2A22`（文字）
  - パンチェッタローズ `#B14A5A`（アクセント）
  - 補助として `#F1E8DC`（薄い枠線）、`#E7CBCF`（淡いローズ）を使用
- **スマホ縦画面ファースト**。最大幅480px、下部固定タブナビ（熟成中 / 新規仕込み / アーカイブ）
- **ブラウザ標準の `alert()` `confirm()` は使わない**。確認はアプリ内モーダル、通知は画面下のトーストで行う（サンドボックス環境でブロックされた実績あり）
- 日数カウントは**仕込み当日を「0日目」**とする
- UI文言はすべて日本語

## 今後の改修候補

- 記録のバックアップ（JSON書き出し / 読み込み）※ブラウザのデータ削除で消えるリスク対策
- 重量変化の折れ線グラフ表示
- 完成予定日が近づいたときの目立つ表示
- 3段階以上の熟成工程への対応（現状は塩漬け→スパイスの2段階固定）

## 依頼者について

- Excel VBA / Access を業務で日常的に使用。プログラミングの基礎知識あり
- 回答は日本語、結論を先に。作業前に PLAN（何をどう変えるか）を提示してほしい
- コードは断片ではなく、コピペでそのまま動く完全な形で示してほしい
